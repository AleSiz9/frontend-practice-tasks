// Класс «Товар» и «Корзина»
// Создайте класс Product с полями name, price, quantity. Добавьте метод getTotalPrice(), возвращающий стоимость с учётом количества.
// Создайте класс Cart, который хранит массив товаров и методы:

// addProduct(product, quantity) — добавить товар (если уже есть, увеличить количество);

// removeProduct(productName) — удалить товар полностью;

// getTotal() — вернуть общую стоимость всех товаров;

// clear() — очистить корзину.
// Используйте принципы инкапсуляции (сделайте поля приватными, если используете современный синтаксис).

// Наследование: Электроника и Одежда
// Создайте базовый класс Item с полями name, price. От него унаследуйте Electronics (добавить поле warrantyMonths) и Clothing (добавить поле size). Переопределите метод getDescription(), чтобы он возвращал строку с описанием, включая специфичные поля. Продемонстрируйте полиморфизм, вызвав этот метод для объектов разных классов.

// Статические методы и свойства
// Реализуйте класс User со статическим свойством userCount, которое увеличивается при создании нового пользователя. Добавьте статический метод getUserCount(), возвращающий количество созданных пользователей. Также добавьте обычные поля name и email.

// Приватные поля и геттеры/сеттеры
// Создайте класс BankAccount с приватным полем #balance. Реализуйте методы deposit(amount), withdraw(amount) и геттер balance, который возвращает текущий баланс. При попытке снять больше, чем есть, выбрасывайте ошибку. Проверьте работу.

// Композиция вместо наследования
// Напишите два класса: Engine (свойства power, type) и Car (свойства model, engine — объект класса Engine). В классе Car создайте метод start(), который выводит сообщение с характеристиками двигателя. Покажите, как композиция позволяет переиспользовать код.