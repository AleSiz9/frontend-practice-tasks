// Одиночка (Singleton)
// Реализуйте класс Logger, который всегда возвращает один и тот же экземпляр. В нём должен быть метод log(message), записывающий сообщения в массив (история логов). Проверьте, что создание нескольких экземпляров через new Logger() возвращает один и тот же объект.

// Фабричный метод (Factory Method)
// Создайте абстрактный класс Transport с методом deliver(). Реализуйте два конкретных класса: Truck и Ship. Затем напишите класс Logistics с фабричным методом createTransport(type), который возвращает нужный объект. Добавьте метод planDelivery(), использующий фабрику. Продемонстрируйте работу для разных типов.

// Наблюдатель (Observer)
// Реализуйте систему подписок: класс NewsPublisher (издатель) с методами subscribe(observer), unsubscribe(observer), notifyAll(data). Класс Subscriber (наблюдатель) должен иметь метод update(data), который выводит полученные данные. Создайте несколько подписчиков и протестируйте рассылку уведомлений.

// Стратегия (Strategy)
// Напишите функцию calculateDiscount(price, strategy), где strategy — это одна из функций-стратегий:

// noDiscount — без скидки;

// tenPercentDiscount — 10%;

// fixedDiscount(amount) — фиксированная скидка (возвращает функцию, которая вычитает заданную сумму).
// Покажите, как можно динамически подменять стратегию.

// Декоратор (Decorator) — практическая реализация
// Создайте базовый класс Coffee с методом cost() (возвращает 50) и description() (возвращает "Кофе"). Напишите декораторы Milk и Sugar, которые принимают объект кофе и возвращают новый объект с изменёнными методами:

// cost() добавляет стоимость добавки;

// description() дополняет описание.
// Пример: let myCoffee = new Sugar(new Milk(new Coffee()));
// Выведите итоговую стоимость и описание.